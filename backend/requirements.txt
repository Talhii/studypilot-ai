fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
langchain-community==0.3.14
anthropic>=0.39.0
# Real document text extraction (PDF / DOCX / PPTX).
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
# Persistence + auth (optional at runtime — the app falls back to in-memory /
# dev-user when these env vars/credentials are absent).
supabase>=2.9.0
firebase-admin>=6.5.0
